# Generated artifacts

Each material run gets a unique directory under `runs/` with configuration, container digest, scheduler record, logs, outputs, metrics, and decision. Large products are not committed, but their checksums and reports are.
