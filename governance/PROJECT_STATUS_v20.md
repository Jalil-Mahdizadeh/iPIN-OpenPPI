# iPIN-OpenPPI project status and execution checkpoint

**Checkpoint date:** 2026-08-08

**Execution environment:** NAISS Arrhenius; every scientific operation must run
through the pinned ARM64 Apptainer image

**Scientific programme state:** accepted `DEC-0020` evidence preserved; final
benchmark endpoint/component partition skeleton authorized under `DEC-0021`;
all pair-level benchmark construction and model work remain prohibited

The authoritative gate is `governance/gates/gate_status_v20.yaml`.

## Authorization now active

`DEC-0021` authorizes exactly one model-free split-construction package. It may
freeze component and endpoint assignments for the immutable 17,000-sequence
universe and aggregate released-positive opportunity summaries. It may not
emit positive-pair identities, candidate pairs, negative or pseudo-negative
labels, or pair-level C1/C2/C3 assignments.

The primary hard partition rule is 30% `local_domain_union`; 30%
`sensitive_fl80_union` is a prespecified fallback only if no primary candidate
passes every frozen criterion. The objective, 4,096-candidate search, seed,
tie-breaking, acceptance floors, balance criteria, and fallback trigger must be
frozen in configuration before production selection.

## Binding interpretation

The component split targets 70% training, 15% development, and 15% protected
test endpoints without component crossing. Future interaction-supervised
training may use training-assigned endpoints only.

C3 means only that both exact frozen sequence endpoints are absent from
interaction-supervised training and their component or components are disjoint
from training under the selected named rule. It does not mean unseen
biological family, unseen homolog/domain, or PLM-unseen protein.

## Immutable parents and closed panels

The accepted eligibility/component and pre-split audits must not be reopened,
recomputed, extended, or modified. The TF-isoform and Lambourne panels remain
external-only and unused. The TF-isoform panel remains unsuitable for training
negatives or any training role, universal-nonbinding claims, prevalence,
calibration, and unseen-endpoint or family benchmarking.

## Continuing hold

The following remain prohibited:

- full candidate-pair materialization or sampling;
- positive/unlabeled indicator rows, negative labels, or pseudo-negatives;
- pair-level C1/C2/C3 labels or final evaluation-pair allocation;
- external-panel integration;
- structural mapping or structure-derived labels;
- prevalence, probability, or calibration claims; and
- model implementation, embedding, training, tuning, selection, evaluation,
  routing, or release.

The split package must return to governance after clean-tree production and
independent validation.
