# iPIN-OpenPPI project status and execution checkpoint

**Checkpoint date:** 2026-08-19

**Execution environment:** NAISS Arrhenius node `n180`; accepted
`ipin-model-arm64_0.1.0.sif`; one NVIDIA GH200 120 GB

**Scientific programme state:** `DEC-0031` accepts and freezes the complete,
independently validated Stage 1 public-training execution and determines that
the frozen prerequisites for a later development release are satisfied;
development remains encrypted and unreleased

The authoritative gate is `governance/gates/gate_status_v30.yaml`.

## Completed Stage 1 execution

Both frozen ESM-2 revisions were acquired into exact, link-free local custody
and the accepted runtime remained hash-identical. Label-blind pooled FP32
embeddings were generated for all 17,000 endpoints for each encoder, with
training-only normalization. The independently validated one-percent repeats
had maximum absolute difference `0.0` for both candidates.

Every mandatory shortcut, graph/degree, sequence/interolog, PLM-linear,
nonlinear no-gate, and partner-gated implementation is frozen and validated.
The exact public P/U objective used all 16,799 P and 2,000,000 U observations
as prescribed, with exact design weights and without negative or pseudo-negative
construction.

All 30 preregistered runs completed on one GPU with zero failures and zero
resumes. The stage executed 150 complete passes, 73,350 optimizer steps, and
exactly 300,000,000 comparisons. It froze 150 checkpoints, 30 selected
checkpoints, and 10 three-seed ensemble definitions. Conservative total compute
was 0.45626144182586964 GPU-hours and governed storage was 14,959,953,220
bytes, both below their ceilings.

## Frozen evidence and validation

The complete training-artifact registry SHA-256 is
`11d7a92d6dd42ca78434783844cbba2ffb05ac789b76eca4399528d0d19ab318`.
It registers 647 unique artifacts totaling 15,124,997,716 bytes. Production
audit passed 12 checks and the clean-room independent validator passed 14,
with zero warnings and zero failures in either report.

Independent validation rehashed every artifact, rebuilt all orders and exact
weights, inspected all checkpoints, scored all selected checkpoints in both
orientations at swap maximum absolute difference `0.0`, confirmed complete
one-GPU offline execution, and found no development, protected, private-key,
temporary, or sensitive-path leakage.

## Scientific interpretation boundary

Only training-monitor values exist. They are optimization diagnostics and do
not select a model or support a generalization, C1/C2/C3, complexity, shortcut,
or biological performance claim. No degree/hub or C1 novel-U result exists yet.
The frozen complexity and model-level kill criteria remain pending development.

## Current hold

Development is encrypted and unreleased. Although its frozen prerequisites are
now satisfied, access requires a separate effective numbered governance
decision. No active executable work package exists. Protected candidates and
truth remain encrypted and evaluator-only.

No further training, adaptive run, checkpoint or feature change, negative or
pseudo-negative construction, benchmark modification, external-panel
integration, residue/interface modelling, or protected action is authorized.
Any later development release must use only the already-frozen scorers and
evaluation procedure and must return to governance before protected test.
