# iPIN-OpenPPI project status and execution checkpoint

**Checkpoint date:** 2026-08-08

**Execution environment:** NAISS Arrhenius; every scientific operation must run
through the pinned ARM64 Apptainer image

**Scientific programme state:** accepted `DEC-0022` endpoint/component split
preserved; pair-level PU-R protocol freeze authorized under `DEC-0023`; pair-row
construction, sample realization, and all model work remain prohibited

The authoritative gate is `governance/gates/gate_status_v22.yaml`.

## Authorization now active

`DEC-0023` authorizes exactly one protocol-only work package. It may freeze and
aggregate-check information cutoffs, evidence visibility, exact C1/C2/C3
assignment semantics, positive withholding, deterministic unlabeled-sampling
rules, metrics, uncertainty, supported auxiliary holdouts, degree/hub strata,
and simple future baselines.

The package may reconstruct the accepted 58,049 released-positive sequence
pairs transiently to validate the rules. It may not persist any pair identity,
candidate row, positive/unlabeled indicator, C1/C2/C3 row, or sampled unlabeled
set.

## Immutable parent and scientific boundary

The 17,000 endpoint assignments and 7,782 30% `local_domain_union` components
frozen by `DEC-0022` are immutable. The primary design remains reference-
sequence positive-unlabeled ranking, and unreported eligible pairs remain
unlabeled.

C3 continues to mean only that both exact frozen sequence endpoints are absent
from interaction-supervised training and component-disjoint from training under
the named frozen rule. It does not mean unseen biological family or PLM-unseen
protein.

Source/study/assay/temporal axes may activate only when their required fields
are complete and scientifically interpretable. Missing or source-confounded
metadata must produce an inactive or descriptive disposition, never an inferred
holdout.

## Closed panels and continuing hold

The TF-isoform and Lambourne panels remain external-only and unused. The
TF-isoform panel remains unsuitable for training negatives or any training
role, universal-nonbinding claims, prevalence, calibration, and unseen-endpoint
or family benchmarking.

The following remain prohibited:

- pair-level benchmark rows or candidate-universe materialization;
- unlabeled-sample realization, negative labels, or pseudo-negatives;
- modification of the frozen endpoint/component skeleton;
- external-panel integration or structural-label work;
- prevalence, probability, biological-precision, or calibration claims; and
- model implementation, embedding, training, tuning, selection, evaluation,
  routing, or release.

The protocol package must return to governance after clean-tree production and
independent validation.
