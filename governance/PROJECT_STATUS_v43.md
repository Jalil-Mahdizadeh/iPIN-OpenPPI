# iPIN-OpenPPI project status: local-representation diagnostic accepted and branch stopped

The public-training-only local-representation diagnostic is complete and
independently validated. The primary local top-four segment cosine scored
`0.5531708398478847` on nested C3, below the matched global same-forward score
of `0.5687588309531323`. The preregistered incremental delta was
`-0.015587991105247556`, so the joint trigger failed.

DEC-0044 accepts the result. Conditional Phase B was not run and is prohibited
under this protocol. The disposition is no incremental coarse local signal and
stop this architecture branch. This does not prove every learned token-aware
model impossible, but any such proposal now requires a new prospective
scientific justification and governance authorization.

Development was not accessed. Protected candidates and truth remain sealed.
The parent DEC-0039 stop disposition and every benchmark freeze remain
unchanged.

The authoritative ledger is `governance/gates/gate_status_v43.yaml`.
