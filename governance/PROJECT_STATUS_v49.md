# iPIN-OpenPPI status: composition competes; matched validation lacks support

Date: 2026-09-11. Authority: DEC-0049.
Supersedes v48 for the new explanatory diagnostic only.

The composition/order diagnostic is complete and numerically verified on the
already examined external BioPlex panels. This is not new validation evidence.
AAC cosine achieves bait-macro concordance 0.627292 / 0.612815 in 293T / HCT116,
versus the locked pair head's unchanged 0.617146 / 0.609358. Pair-minus-AAC
intervals [−0.033364, +0.017102] / [−0.029204, +0.028679] cross zero. No new
learned-model superiority, statistical inferiority or equivalence is established.

Native 3-mers outperform the composition-preserving shuffled statistic by
0.038881 / 0.034970, with paired lower bounds 0.030159 / 0.020778. This passes
the prospectively fixed diagnostic rule; shuffle-half macro differences
0.000060 / 0.000731 pass the sensitivity check. But AAC is stronger than the
shuffle comparator. The increment concerns this particular anchor-ranking
statistic, not superiority over all order-free controls or a binding mechanism.
Native-versus-shuffled endpoint-balanced quartet intervals cross zero. Even
AAC surpasses the additive quartet null in both cells. Composition can encode
biology; no causal or learned-head attribution is supported.

The registered matched-partner challenge is infeasible. Composition/length
calipers with at least five U retain only 22 / 3 positives on 22 / 3 baits,
122 / 17 comparisons; the additional direct-similarity calipers retain none.
All fold-level support floors fail. HCT116 matched intervals are withheld
because only 1,561/2,000 component draws are valid. Sparse matched point results
and same-support unmatched references are retained without evidentiary promotion.
No caliper tuning, repeat scoring or additional permutations were performed.

The current bottleneck is a sufficiently informative comparison set. Next
priority is an opportunity/construct and matched-support feasibility audit for
a dense, independent direct-binary candidate panel, before scoring and with
confirmation separated from pilot design. No architecture sweep is justified.
The DEC-0048 all-comparator external superiority gates still fail; the original
gated/complexity stop remains. There is no new-model novelty or general-purpose
partner-specific binding-prediction claim.

Execution used only original public sequences, frozen panels/scores and original
component draws, inside the pinned ARM64 data image. 761,600 permutations and
1,706,845 new score values; zero fits, zero encoder extraction, zero new source
interaction acquisition. No development evaluation, protected packages/keys,
quarantined panels, old artifact edits, or raw-data redistribution. Inherited
homology, mapping, construct, co-association and unknown-opportunity limitations
remain, including ISSUE-0015. This does not authorize protected evaluation.

Verification: 451 CPU tests passed before and after, plus the existing GPU
regression (452 distinct cases, 36 new). The separate same-author audit checks
all new pair scores, all AAC vectors, every match membership, every point and
interval/flag, 64 endpoint shuffle reconstructions (4,096 permutations),
16 anchor/matched and 2,000 quartet draws per cell. Maximum score error
1.1657341758564144e−15. Prior DEC-0046/47/48 closures are unchanged. This is
numerical validation, not external peer review or independent replication.

Report: `docs/reports/m1/M1_Composition_Order_Challenge_v1.md`.
Readout: `artifacts/results/composition_order_challenge_v1/RESULTS.json`.
Gate ledger: `governance/gates/gate_status_v49.yaml`.
