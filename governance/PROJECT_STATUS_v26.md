# iPIN-OpenPPI project status and execution checkpoint

**Checkpoint date:** 2026-08-18

**Execution environment:** NAISS Arrhenius; governance validation must use the
checksum-pinned ARM64 data SIF, while a future model SIF is not built or
authorized

**Scientific programme state:** `DEC-0026` pair artifacts remain accepted and
immutable; `DEC-0027` authorizes a bounded model-governance and baseline/
training-protocol design only; all model execution and all development/
protected access remain prohibited

The authoritative gate for this active work package is
`governance/gates/gate_status_v26.yaml`.

## Preflight disposition

The `RESUME-002` preflight was completed on 2026-08-18 from `main` at
`55be4cd4f43659acf32423580b94732aa7e38041`, equal to freshly fetched
`origin/main` with divergence `0 0`. All prescribed manifest sidecars and
direct container/training/ciphertext hashes matched. The eight prescribed
safety modules passed 26 tests inside `ipin-data-arm64_0.1.2.sif`.

No sealed content was opened, no private key was accessed, and no protected or
development identity was inspected.

## Authorization now active

`DEC-0027` authorizes only `model_governance_and_baseline_training_protocol_v1`.
It must freeze a small auditable PLM candidate set, pooled-embedding strategy,
mandatory shortcut and sequence baselines, a design-weighted P-versus-U
ranking objective, one simple symmetric partner-conditioned head, bounded
ablations/search/seeds, checkpoint and stopping rules, development release and
model selection, reporting/stratification/novel-U sensitivity, complexity
criteria, and model-level kill rules.

The work package may use public model-card and repository metadata. It may not
download model files or instantiate model code.

## Immutable scientific boundary

The following remain unchanged and must not be reopened:

- 17,000 exact UniProt `2026_02` reference-sequence endpoints;
- frozen 40%/30%/20% similarity inventories and the 30%
  `local_domain_union` leakage graph;
- the 7,782-component 11,900/2,550/2,550 split;
- C1/C2/C3 identity, exposure, quarantine, sampling, weight, metric,
  uncertainty, and claim semantics;
- 16,799 public training positives and 2,000,000 public training-U rows; and
- encrypted development, protected-candidate, and protected-truth packages and
  their custody procedure.

Unlabeled observations are not negatives. A future pairwise ranking objective
may use them only as weighted comparison observations.

## Continuing hold

This authorization does not permit model/checkpoint/tokenizer acquisition,
model-cache population, model-container construction, embeddings, feature
caches, baseline/model implementation, training, tuning, checkpointing,
development release, model selection, scoring, or evaluation.

Protected candidates/truth, private keys, external diagnostic panels,
structures, residues/interfaces, teachers, negatives/pseudo-negatives, new
pair rows, the full candidate universe, and probability/prevalence/calibration/
biological-precision/unseen-family/PLM-unseen claims remain outside authority.

The protocol must be independently validated and accepted by a new numbered
decision. That acceptance will still not authorize training; a subsequent
numbered implementation authorization is mandatory.
