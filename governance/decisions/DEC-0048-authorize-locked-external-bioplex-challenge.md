# DEC-0048: Locked external BioPlex evidence challenge

Date: 2026-09-11. Status: bounded authorization from the user's instruction
to continue with the best next step, report, update docs, commit and push.

The principal remaining gap after DEC-0047 is independently generated evidence,
not another architecture or within-source sweep. Authorize the published 2021
BioPlex 293T/HCT116 networks, their directed-edge and bait lists, solely for a
separate reference-sequence co-association PU recovery challenge. The six exact
official assets are allowlisted in `configs/external_bioplex_challenge_v1.yaml`.
Acquire through HTTPS inside the pinned data image, retain timestamps, response
metadata, local checksums and immutable raw snapshots. Do not acquire unpublished
quarterly releases, unfiltered MS data, or other sources. No raw redistribution.

Freeze the scientific protocol and the existing DEC-0047 `purged20` pair and
linear-unary checkpoints before acquisition. New fits, encoder extraction,
hyperparameter selection, threshold search and replacement data are prohibited.
Only the original 11,900 public-training sequence endpoints and their original
three component folds are eligible. Reuse identity-verified embeddings and
detected-homology-purged normalization/fitting graphs. Source rows outside that
public allowlist may be counted for mapping attrition but not scored or emitted.

Permit new external positive identities on that public endpoint support only.
Remove every already released parent public positive from external targets;
reuse existing released U rather than constructing a new candidate universe.
Never query evaluator roles, development/protected pairs, source packages, key
escrow, or the quarantined Lambourne/TF-isoform panels. No existing scientific
artifact is amended. This work does not authorize protected evaluation.

This is external experimental evidence on reused public endpoints, not a new
wet-lab experiment, fully independent benchmark construction, direct-binding
validation, or a complete attempted/evaluable assay universe. It cannot revive
the stopped complex-architecture claim. Both cell-line outcomes and all locked
comparators must be reported even if the result is negative or infeasible.

The user explicitly authorizes publishing the implementation, aggregate results,
concise report and relevant documentation via ordinary commit and push.
