# DEC-0044: Accept the local-representation diagnostic and stop the branch

**Date:** 2026-08-19

**Status:** Accepted and effective

Accept the complete DEC-0040/DEC-0041 public-training-only diagnostic and its
independent validation.

The frozen primary local top-four segment score reached nested-C3 HT
concordance `0.5531708398478847`, but the matched global score from the same
ESM forward pass reached `0.5687588309531323`. Their delta was
`-0.015587991105247556`, failing the preregistered minimum of `+0.01` despite
passing the separate absolute-local minimum of `0.51`. The joint trigger is
false.

The governance disposition is therefore:

> No incremental coarse local-representation signal; do not run conditional
> Phase B; stop this architecture branch.

The Phase B low-capacity learned model is prohibited under this protocol. It
was not run. The result does not revive the stopped pooled partner-gated claim,
authorize protected evaluation, or establish a general impossibility result
for every token-aware architecture.

Production validation passed `7/7` checks. A later standalone implementation
passed `13/13` independent checks and exactly reproduced all `609,700` scores,
point metrics, `1,400` bootstrap values, intervals, and the trigger. Development
was not accessed. Protected ciphertexts were hash-checked only and remained
sealed. No negative, pseudo-negative, external panel, adaptive scorer, or
training run was introduced.

ISSUE-0012 and ISSUE-0013 are closed as fail-closed numerical implementation
incidents corrected before production of their affected artifacts. Their
corrections changed no scientific formula, threshold, retained embedding,
benchmark semantic, or information boundary.

Any future learned residue-, domain-, or token-aware proposal is a new
scientific work package. It requires a fresh prospective protocol and numbered
decision, and may not use already spent development information or protected
material for architecture design.

All benchmark freezes and the DEC-0039 parent development disposition remain
unchanged.
