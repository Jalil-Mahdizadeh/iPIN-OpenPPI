# iPIN-OpenPPI project status and execution checkpoint

**Checkpoint date:** 2026-08-19

**Execution environment:** NAISS Arrhenius interactive node `n180`; one visible
NVIDIA GH200 120 GB GPU; exact single-GPU Stage 1 budget

**Scientific programme state:** `DEC-0029` authorizes bounded executable Stage
1 work under the unchanged, accepted `DEC-0028` protocol; scientific embedding
and training wait for separate acceptance of the newly built model runtime

The authoritative gate is `governance/gates/gate_status_v28.yaml`.

## Authority and preflight

The active-session project-owner instruction authorizes execution of the
complete frozen Stage 1 public-training work package. The `RESUME-003`
preflight ran at
`f573ea411233f42d8f3d9a30e640baa2eef10363` with clean, identical local and
remote `main`. Both immutable protocol evidence sidecars validated, every
prescribed repository, public-data, and sealed-ciphertext hash matched, and the
exact pinned-container safety suite passed 53 of 53 tests. Sealed packages were
hashed only; none was opened.

## Authorized stage

`DEC-0029` permits exact-revision ESM-2 150M/650M acquisition, project-local
hash-pinned custody, construction and independent qualification of
`ipin-model-arm64_0.1.0.sif`, implementation and testing of every frozen Stage 1
method, both complete 17,000-endpoint embedding sets, and the exact 30-run
public-training-only matrix.

Runtime acceptance is an intermediate hard gate: qualification may use only
synthetic fixtures, and a separately numbered decision must accept the SIF
before it processes a scientific endpoint or pair. Once accepted, execution is
limited to one GH200 at a time, five fixed passes, the three frozen seeds and
recipes, at most 300 million comparisons, 100 GPU-hours, and 100 GiB.

## Immutable scientific boundary

The accepted endpoint universe, reference sequences, similarity graphs,
components, partition, C1/C2/C3 definitions, PU-R objective, public P/U rows and
weights, pair artifacts, metrics, and encrypted packages are unchanged.
Unlabeled is not a negative class. No negative or pseudo-negative artifact,
new row, resample, full universe, external panel, structure, residue/interface
feature, or additional model candidate is authorized.

Development and protected packages remain encrypted and inaccessible. No
development score, selection, protected prediction, private key, or sealed
identity is authorized in this stage.

## Required execution return

Before any later development release can be considered, all 30 runs must be
complete or explicitly failed closed; successful runs must have training-only
selected checkpoints and frozen three-seed ensembles; and all inputs, code,
runtime, embeddings, configurations, logs, checkpoints, metrics, orders,
budgets, and dispositions must enter one hash-pinned training-artifact registry.

Production validation and an independently implemented validator must confirm
embedding integrity, visibility, objective algebra/weighting, exact method
definitions and symmetry, run completeness, reproducibility and checkpoint
rules, artifact custody, budget compliance, and absence of leakage. The stage
then returns through a numbered results decision stating whether every frozen
development-release prerequisite has been satisfied. Completion alone does not
release development.
