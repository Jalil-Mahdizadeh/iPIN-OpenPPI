# Test policy

- `unit/`: pure transformations and schema rules.
- `integration/`: bounded containerized workflows.
- `fixtures/`: small, redistributable deterministic inputs.
- `reproducibility/`: repeat-run, manifest, checksum, and frozen-split checks.

Tests that import project scientific dependencies run inside an accepted SIF.
