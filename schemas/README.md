# Data contracts

Versioned, reviewable schemas live here. Large source-native, canonical, and
derived datasets remain under `data/` and outside Git; every generated table
must embed the applicable schema name, version, and SHA-256 in its metadata.

The active evidence-warehouse contract is
`schemas/warehouse/evidence_warehouse_v1.yaml`.
