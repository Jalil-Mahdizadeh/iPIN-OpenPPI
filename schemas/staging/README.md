# Source-native staging schemas

These contracts cover source material that is useful for audit and later
reconciliation but is not itself a canonical interaction-evidence record.
Source-native values remain strings unless the provider format makes a type
unambiguous. Classification into labels is outside this layer.
