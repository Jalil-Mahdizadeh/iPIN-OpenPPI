# Source manifests

Each source receives a versioned YAML or JSON manifest before acquisition. Minimum fields are source name, release, upstream URL, retrieval timestamp, expected files, checksum algorithm, license/terms record, parser version, and allowed redistribution form.
