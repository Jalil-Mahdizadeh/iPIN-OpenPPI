# Derived data

Features, clusters, audit annotations, embeddings, and other reproducible transformations of canonical inputs live here. Every derived dataset must identify its input-manifest and code/config hashes.
